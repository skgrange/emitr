language: r
sudo: required
warnings_are_errors: true
cache: packages

env:
  global:
    - _R_CHECK_FORCE_SUGGESTS_=FALSE

r_packages:
  - devtools

# Install development packages
r_github_packages:
  - skgrange/threadr
  - skgrange/databaser
  - skgrange/envirologgerr
